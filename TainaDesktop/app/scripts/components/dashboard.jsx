import React from 'react';

module.exports.create = (taina) => {
  class Login extends React.Component {
    render() {
      return (
        <h2>Dashboard</h2>
      );
    }
  }

  return Login;
};
